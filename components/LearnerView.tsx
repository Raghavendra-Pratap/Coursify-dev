'use client'

/**
 * Stub for root type-check only. Real component in coursify-app/components/LearnerView.tsx.
 */
export function LearnerView({ courseId, userId: _userId }: { courseId: string; userId: string }) {
  return <div data-stub="LearnerView">{courseId}</div>
}
