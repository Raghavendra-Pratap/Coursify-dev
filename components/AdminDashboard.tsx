'use client'

/**
 * Stub for root type-check only. Resolves @/components/AdminDashboard when root type-checks coursify-app.
 * Real component is in coursify-app/components/AdminDashboard.tsx.
 */
export function AdminDashboard({ userId }: { userId: string }) {
  return null
}
