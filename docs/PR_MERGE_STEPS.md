# PR / merge notes

**Status**: The branch `feature/learner-mode-advancements` was **merged directly into `develop`** (no pull request). All changes (course import from sheet, learner mode, Q&A, notifications, My Notes, ratings, build fixes) are on `develop` and deploy from there.

For future feature branches: create a PR from the branch to `develop` on GitHub, or merge locally with `git checkout develop && git merge <branch> && git push origin develop`.
