# What was lost in commit 5230d267 (vs e359e8bd)

Comparison: **e359e8bd** (Video players: YouTube + Drive support, progress sync, port config) → **5230d267** (our push).

## 1. Port config (removed)
- **config/app.config.json** – had `{"port": 3000}` (you wanted 3001)
- **scripts/run-dev.js** – read config and ran `next dev -p <port>`
- **scripts/run-start.js** – read config and ran `next start -p <port>`
- **package.json** – `"dev": "node scripts/run-dev.js"`, `"start": "node scripts/run-start.js"` (reverted to plain `next dev` / `next start`)

## 2. CreateCourse.tsx (removed or reverted)
- **normalizeSnapshotForCompare()** – version/change detection for course snapshots
- **getReadingEmbedUrl()** – document URLs to embed (Google Docs preview, etc.)
- **getFormEmbedUrl()** – Google Form embed URL for quiz/form in combined viewer
- **State:** `lessonPreviewMode`, `previewPopupContent`, `combinedSegmentIndex`, `combinedPlaybackSeconds`, `combinedPlaying`, `combinedYtPlayerReady`, `combinedYtPlayerRef`, `combinedDrivePlayerReady`, `combinedDriveVideoRef`
- **driveManualTime** state and related Drive reset logic

## 3. LessonVideoPlayer.tsx (removed or reverted)
- **onProgress** prop – reported (progress 0–1, durationSeconds) to parent for segmented bar
- **segmentBoundsRef** – current segment bounds for progress calculation
- **Cleanup on unmount:** `setReadyPlayer(null)`, `setYtReady(false)` (fixes “progress worked once then stopped” on segment change)
- **YouTube poll:** progress reporting to parent; **effectiveDuration** logic for bar
- **handleEnded:** guard so we don’t mark complete for near-zero-length segments
- **segmentDuration** in effect dependency array
- **Direct video:** progress report interval to parent; **segmentLen** guard before markComplete

## 4. TakeCourse.tsx (removed or reverted)
- **currentVideoProgress** / **currentVideoDurationSec** state
- **Effect** that resets progress when `currentStepIndex` changes
- **onProgress** passed to LessonVideoPlayer
- **Bottom bar:** segment name + duration (e.g. “X min” / “Y sec”)
- **Segmented progress bar** (blue = completed/current, white = remaining, grey = upcoming)
- **Dark video area** styling (e.g. bg-gray-900) and layout (step label + “Watched” in header; single 16:9 player area)

---

**Conclusion:** The push combined our new work (combined preview footer, Drive buffer, doc) with already-staged changes that reverted the above. Restoring from e359e8bd and re-applying our changes will recover the lost behavior.
